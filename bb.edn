{:paths ["src" "test"]
 :tasks
 {test {:doc "Runs the tests with bb"
        :requires ([clojure.test :as t])
        :task (do (require 'babashka.publish-jar-test)
                  (let [{:keys [fail error]} (t/run-tests 'babashka.publish-jar-test)]
                    (System/exit (+ fail error))))}}}
